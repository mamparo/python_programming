from math import *

file  = pickAFile()
sound = makeSound(file)
sample = getSamples(sound)
arr = []
width = getLength(sound) % 400
pic = makeEmptyPicture(400, wid)

for sample in range(0, getLength(sound)):
  if sound.getSampleValue(sample) == 0:
    arr.append(1)
  elif sound.getSampleValue(sample) < 0:
    arr.append(2)
  else:
    arr.append(3)

for i in arr:
 if arr[i] == 1:
   