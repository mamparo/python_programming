newFile = pickAFile()
picture = makePicture(newFile)
wid = getWidth(picture)
heigh = getHeight(picture)
a = 0
b = 0
samp = wid * heigh
sound = makeEmptySound(samp)

purple = makeColor(128, 0, 128)
teal = makeColor(0, 128, 128)

for x in range(0, wid):
  pix = getPixel(picture, a, b)
  if pix == red:
    